package com.demo;

import org.springframework.context.annotation.Configuration;

@Configuration
public class VaultConfig {
    // Using direct RestTemplate approach in DemoController
    // This avoids Spring Vault configuration complexities
}
